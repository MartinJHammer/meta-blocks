export const uppercase = (x: string) => x.toUpperCase();

export const lowercase = (x: string) => x.toLowerCase();

export const upperCaseArray = (data: string[]) => data.map(uppercase);

export const lowerCaseArray = (data: string[]) => data.map(lowercase);


